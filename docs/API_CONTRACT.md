1. POST /api/v1/waste/classify
   → Waste image classify

2. GET /api/v1/bins
   → Saare bins

3. GET /api/v1/bins/{bin_id}
   → Ek bin ki details

4. POST /api/v1/sensor-readings
   → Bin fill-level/sensor data save

5. GET /api/v1/collection-priority
   → Kaunse bins pehle collect karne hain

6. GET /api/v1/dashboard
   → Dashboard numbers

7. GET /api/v1/waste/stats
   → Waste/segregation statistics

8. GET /api/v1/recommendations
   → Recommendations